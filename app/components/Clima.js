import { StyleSheet, Text, View } from 'react-native'

const Clima = () => {
    return (
        <View>
            <Text>Muestra el clima de la Ciudad seleccionada del componente 'List'</Text>
        </View>
    )
}

export default Clima

const styles = StyleSheet.create({

})
